# Claude Code — personales (no commitear)
CLAUDE.local.md
.claude/settings.local.json

# Dependencias
node_modules/

# Build / output
dist/
build/

# Tauri / Rust
src-tauri/target/
# Sidecars: se bajan con `bun run sidecar:fetch`, no se commitean (~115 MB)
src-tauri/binaries/
# whisper-cli + sus DLLs de ggml, mismo script (~8 MB)
src-tauri/resources/whisper/

# Tests
coverage/
playwright-report/
test-results/

# Logs y temporales
*.log
.DS_Store
